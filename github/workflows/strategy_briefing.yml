name: Strategy Briefing Automation

on:
  schedule:
    # يعمل مرتين يومياً (6:00 صباحاً و 6:00 مساءً بتوقيت UTC)
    - cron: '0 6,18 * * *'
  workflow_dispatch: # يتيح لك تشغيل السكربت يدوياً بنقرة زر للتجربة

jobs:
  run-briefing:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests feedparser

      - name: Execute Strategy Briefing Script
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          CHAT_ID: ${{ secrets.CHAT_ID }}
        run: python main.py
